spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/conduit
    driver-class-name: org.postgresql.Driver
    username: conduit
    password: conduit
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true